{
  "name": "claudex",
  "version": "0.1.0",
  "private": true,
  "description": "Launch the genuine Claude Code harness with GPT-5.6 Sol through Codex OAuth.",
  "type": "module",
  "bin": {
    "claudex": "bin/claudex"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepack": "pnpm run build"
  },
  "engines": {
    "node": ">=22.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.33.1"
}
